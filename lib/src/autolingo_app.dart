import 'package:flutter/widgets.dart';

class AutoLingoApp extends StatelessWidget {
  final Widget child;

  const AutoLingoApp({
    super.key,
    required this.child,
  });

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
